 #include <iostream> // aberturas das minhas bibliotecas
#include <locale.h>    // outra bibliteca
using namespace std;  // isso é obligatorio

int main()
{
    setlocale(LC_ALL, "portuguese"); //comando para poder usar o (´^~`Ç) entre outros
float h, p, fmlw;  // float = numero decimal ou real e aqui vocé declara as varias que vc vai usar
string sexo; //

 cout << "digite seu genero..:" << endl;
 cin>> sexo;
 cout << "digite sua altura..:"<< endl;
 cin>> h;
 cout << "digite seu peso..:";
 cin >> p;

 fmlm =(72.7 * h ) - 58.0;
 fmlw = (62.1 * h) - 44.7;
 if ( "sexo" == femenino){
    cout << "digite sua altura";
    cin>> h;
    fmlw = (62.1 * h) - 44.7;
 }else if (fmlm )
}
